# Advent Of Code 2025

Solutions mostly in bash, with some python when bash was too annoying.

Solutions are by no means efficient but are rather optimised to be "bash-ish" so lots of data in flight and pipelines.

Some solutions rely on some auxiliary programs in `./bin`.
